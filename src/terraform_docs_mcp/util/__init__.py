"""Small, independent helpers with nothing in common but having no better home.

No re-exports here -- callers import each submodule directly
(`terraform_docs_mcp.util.git`, `...util.strenum`, `...util.handle_broken_pipe`),
matching this project's existing package-root-is-a-facade convention.
"""
